# 07 — 支持临时面板、Pinned 与 Spaces

**What to build:** 用户可以在鼠标附近使用自动消失的临时面板，也可以将其 Pin 成当前 Space 内可移动、可调整大小的常驻技能面板。

**Blocked by:** 06 — 通过三键组合呼出并键盘操作面板.

**Status:** done

- [x] 临时面板靠近鼠标并保持在可见屏幕区域。
- [x] 外部点击、Escape、重复快捷键和成功插入按规则关闭临时面板。
- [x] Pinned 面板在当前 Space 内保持打开、可移动、可调整大小。
- [x] Pin 状态不跨应用重启，也不跟随所有 Spaces。
- [x] Pinned 时快捷键打开独立临时面板，不移动原 Pinned 面板。
- [x] 技能库不显示工具栏，只保留左侧四元素与右侧 Skill 列表；Pin 和管理操作位于菜单栏菜单。
